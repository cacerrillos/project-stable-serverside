language: csharp
dist: trusty
sudo: required
sudo: required
mono: none
dotnet: 1.0.1
solution: Project\ Stable.sln
before_script:
  - dotnet restore
script:
  - dotnet build -c Release
  - cd StableAPIHandler\ Tests
  - dotnet test
